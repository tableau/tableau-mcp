# design

Design docs for non-trivial changes to this repo.

- `specs/` — the "what and why": design rationale, requirements, trade-offs.
- `plans/` — the "how": task-by-task implementation plans that execute a spec.

Not every change needs one of these — reserve them for work substantial enough to benefit
from a written design before implementation.
