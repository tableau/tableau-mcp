*.local
.env
.env.*
.vscode/
build/
cache/
node_modules/
junit/
coverage/
test-logs/
config.json
env.list
manifest.json
sea-prep.blob
tableau-mcp
tableau-mcp.dxt
tableau-mcp.exe
tableau-mcp.mcpb
*.pem
*.tgz
*.tar.gz
*.zip

# MCP Apps
src/web/apps/dist/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/blob-reports/
/playwright/.cache/
/playwright/.auth/

# Local agent scratch under .work/ — ignore everything except the committed
# scaffold (specs/TEMPLATE.md + the .gitkeep placeholders). See the agent-os
# repo contract (#410).
.work/*
!.work/specs/
!.work/reports/
!.work/tickets/
.work/specs/*
.work/reports/*
.work/tickets/*
!.work/specs/TEMPLATE.md
!.work/**/.gitkeep

# Stashed tools kept for later (not part of the build)
.stash/

# Local handoff/scratch notes and POCs — kept on disk, never committed.
handoff/
